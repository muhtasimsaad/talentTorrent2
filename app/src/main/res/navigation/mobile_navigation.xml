<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_jobs">

    <fragment
        android:id="@+id/navigation_jobs"
        android:name="com.example.talenttorrent.ui.jobs.Jobs"
        android:label="@string/title_home"
        tools:layout="@layout/browse" />

    <fragment
        android:id="@+id/navigation_browse"
        android:name="com.example.talenttorrent.ui.browse.Browse"
        android:label="@string/title_dashboard"
        tools:layout="@layout/jobs" />

    <fragment
        android:id="@+id/navigation_explore"
        android:name="com.example.talenttorrent.ui.explore.Explore"
        android:label="@string/title_notifications"
        tools:layout="@layout/explore" />
</navigation>